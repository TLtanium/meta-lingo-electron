# Data Directory

This directory stores:
- `corpora/` - Uploaded corpus files (text, audio, video)
- `database.sqlite` - SQLite database for metadata

This directory is automatically created and managed by the application.

